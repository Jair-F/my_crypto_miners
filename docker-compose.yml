services:
  my_crypto_miners:
    # image: jair_f/my_crypto_miners:latest
    image: my_crypto_miners:latest
    container_name: my_crypto_miners
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: no
